import { Meta, Markdown } from "@storybook/addon-docs/blocks";
import install from "../../content/generated/knowledgebase/general/install.md?raw";

{/* Source of truth is the MCP knowledgebase — this page renders it verbatim so
    Storybook, the site and the agent docs never drift. */}
<Meta title="Guides/Installation" />

<Markdown>{install}</Markdown>
