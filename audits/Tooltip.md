# Tooltip — Token Audit

All token references verified against `packages/tokens/generated/tokens.css`.

- Bubble: `semantic.color.content.primary` (bg), `semantic.color.content.inverse-primary` (text),
  `radius-8`, `space-8` padding/gap.
- Lead-icon circle: `base.color.white-alpha.10`. Shadow: Level 2 (`0 4px 12px #272B3314`).
- Type: `font-size.tag-11` / `font-line-height-16`. Tail drawn in the bubble colour.

No missing tokens.
