# Floating Action Button — Token Police audit

- 0 raw colour values in component CSS.
- Surface, selected, foreground, and elevation values resolve through generated tokens.
- Uses a native button with a visible keyboard focus state.
- The Figma-authoritative Coral/white Added state is preserved for visual parity. It fails the 4.5:1 text contrast threshold and is explicitly excluded from automated contrast checks pending a Figma token decision.
