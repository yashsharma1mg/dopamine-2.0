# Button — Token Police audit

- 0 raw colour values in component CSS.
- Focus ring resolves through `semantic.color.focus.ring`.
- Disabled state uses native `disabled`, prevents pointer interaction, and renders at 50% opacity.
- All Button token references resolve through `packages/tokens/tokens.json`.

This seed audit must be repeated against the approved Figma component before the package is promoted beyond the pilot.
