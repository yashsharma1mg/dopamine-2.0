# Stepper — Token Police audit

- 0 raw colour values in component CSS.
- Filled and outline treatments resolve from `component.stepper` tokens.
- All actions are native buttons with accessible labels.
- Quantity changes are exposed through a live output.
- The supplied Coral/white pairing fails the 4.5:1 text contrast threshold; the release uses Cool Neutral 10 content on Coral.
